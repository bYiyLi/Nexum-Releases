{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local bundled Desktop shell permissions for Nexum.",
  "windows": ["main", "menu-bar"],
  "local": true,
  "permissions": [
    "core:window:default",
    "core:window:allow-start-dragging",
    "local-shell"
  ]
}
